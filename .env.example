# For SQLite, you can use a file path:
# DATABASE_URL="file:./dev.db"
DATABASE_URL="file:./dev.db"

# Web Push (task reminders when tab is closed/background). Generate with: node -e "const w=require('web-push');const k=w.generateVAPIDKeys();console.log('VAPID_PUBLIC_KEY='+k.publicKey);console.log('VAPID_PRIVATE_KEY='+k.privateKey);"
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=   # same as VAPID_PUBLIC_KEY
# VAPID_MAILTO=mailto:support@example.com
