#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Bump patch version before pushing
echo "🚀 Bumping version before push..."
bun run version:patch

# Add the updated package.json to the commit
git add package.json

echo "✅ Version bumped and package.json staged for commit"
